<?xml version="1.0" encoding="utf-8"?>
<!--
    Permite trafico HTTP (sin TLS) solo hacia el backend local de desarrollo.
    10.0.2.2 es como el emulador de Android ve el "localhost" de tu PC.
    Si despliegas la API con HTTPS en un dominio real, esto ya no hace falta.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
